# Direct dependencies, pinned to the versions verified on Python 3.12.
# The container is python:3.12-slim and Railway builds from it — keep the local
# virtualenv on 3.12 so resolution matches.
#
# To upgrade: bump a pin here, reinstall, run the app, then commit.
#   ./.venv/bin/pip install -r backend/requirements.txt

fastapi==0.141.1
uvicorn[standard]==0.52.4
httpx==0.28.1
pandas==3.0.5
numpy==2.5.3
aiosqlite==0.22.1
# MCP SDK v2 — the API differs from every v1 example (`mcp.server.MCPServer`, not
# `FastMCP`). Brings httpx2, which is a separate distribution from the httpx pinned
# above; both are installed and neither upgrades the other.
mcp==2.2.0
